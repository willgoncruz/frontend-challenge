import { createContext } from "react";

export const SearchContext = createContext('SearchStore');
export const DetailsContext = createContext('DetailsStore');
export const DiscoverContext = createContext('DiscoverStore');